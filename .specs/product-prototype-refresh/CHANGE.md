# CHANGE: 竞品调研融合后的产品原型与界面原型文档

- **Change ID**: product-prototype-refresh
- **创建日期**: 2026-09-22
- **来源 Issue**: MALIZHI-6《项目原型文档审核》
- **路径建议**: 中等（REQUIREMENT → 2a-ui-design → TASK → DEV(仅文档/原型层) → TEST → REVIEW → INTEGRATION）
- **状态**: active（G1 需求门 4 专家投票进行中）

---

## Why（为什么做）

1. **产品没有一份独立的原型事实源**：仓库内（main 与全部分支）不存在 `product-design.html` 或任何原型文档——界面设计隐含在 32 个页面代码里，产品能力描述只有 README.md。没有可评审、可对照、可挂竞品结论的"产品原型 + 界面原型"层。
2. **既有文档与代码已严重漂移**：`.specs/README-CONSISTENCY-REPORT.md`（2026-07-06）实测 README 10 项数据 8 项不准（准确率 20%）；`.specs/CONTEXT.md` 证实代码持续增长而文档未动（页面 20→32、服务 20→25、路由 24→27、组件 25→28，新增控制面/域管理/告警/LLM 提供商等模块 README 均未记载）。融合竞品结论前必须先校准事实底稿，否则新结论贴在过时画面上。
3. **产品能力矩阵缺外部参照**：AgentFlow 定位「通用 Agent 编排与管控平台」（`CLAUDE.md`），相对同类产品（issue 点名 Multica、Buzzz、OpenWorker、AgentOS 等）能力是否完整，从未做过系统比对；需求方明确要求调研并找出可补充进本项目的能力缺口。

## What（做什么）

三个工作包，全部落在文档 / 原型层，串行推进（W3 以 W1/W2 产物为输入），与 issue 三步诉求一一对应：

- **W1 现状审核**：以 `README.md`、`.specs/CONTEXT.md`、一致性报告为文档侧输入，以 `frontend/src/pages`（32）、`components`（28）、`stores`（11）、`backend/routes`（27 模块 / 153 端点）为代码侧事实源，产出「产品能力现状事实底稿 + 文档漂移差异清单」。
- **W2 竞品调研（本 change 只锁定清单与口径，逐项深挖归 1-requirement）**：至少覆盖 Multica、Buzzz、OpenWorker、AgentOS 四个产品（"等"的扩展候选需先列清单经人工确认），统一口径产出能力缺口清单，每条含【来源 URL】【能力描述】【适配初判：值得引入 / 观望 / 不适配 + 理由】。
- **W3 融合产出原型**：一份产品原型 + 界面原型文档（默认 `product-design.html`，零外部依赖、浏览器直开，落 `.specs/product-prototype-refresh/`；若人工提供既有稿则按其位置融合）。在 W1 事实底稿之上呈现全部能力域与界面原型，并叠加竞品结论；每条新增带「来源 = 审核发现 N / 调研结论 N」可追溯标注。

## 视觉调性（前端项目必填）

- **选定**：以既向前端实现为基线——Tremor 3.18 + Tailwind 3.4 + `src/styles/tokens.css` 的管控台式（深色、信息密度优先）调性。
- **理由**：诉求是「融合进既有产品 / 界面原型」，界面原型应贴近真实实现的呈现以便评审对照，而非另起一套新皮肤（步骤 0.6 强偏好例外：不重选，直接锁定既有体系）。
- **参考产品**：第一参照 = 本项目运行界面（`frontend/` 实际页面）；外部密度/层级参照 Linear、Vercel、Grafana 类管控台。
- **明确排除**：高饱和营销落地页调性（管控平台非售卖页）；C 端沉浸式/大留白调性（无大众终端用户场景）。
- ⚠️ `code-kit/reference/ui-aesthetics.md` 九卡库在本仓库未安装（HEAD 即「去掉code-kit标识」），本字段未按卡片编号预选；若人工选择"新建原型"并想要新调性，在 2a 提出后修正。若人工提供 `product-design.html` 既有稿，以该稿调性为准。

## 影响面

- [x] 影响 `REQUIREMENT.md` —— 竞品能力缺口在 1-requirement 蒸馏为需求条目 / AC（含调研深挖）
- [ ] 影响 `DESIGN.md` / 引入新 ADR —— 本 change 不改代码；过审能力若要代码实现，另开新 change
- [ ] 影响现有 AC —— 原型文档此前无 AC，无可改
- [ ] 影响数据模型 / 迁移
- [ ] 影响外部 API 兼容性
- [ ] 仅修复 bug，无范围变化

**写盘边界**：仅 `.specs/product-prototype-refresh/**`（调研产物、需求产物、原型 HTML/文档）+ 人工拍板后的 `product-design.html` 所在路径。`backend/`、`frontend/src/` 零写入。

## 范围排除（这次不做）

- **不改任何 L1 业务代码**（`backend/`、`frontend/src/`）：调研发现再"值得引入"，本 change 也只写进原型文档与需求；代码实现一律走新 change（R16.1/R7.1），杜绝范围悄悄扩。
- **不重写 `README.md`**：漂移已由既有 `.specs/README-CONSISTENCY-REPORT.md` 留档；README 与新原型文档的一致性同步已留档议题 `docs-drift-resync`（见 STATE.md），另期处理。
- 不做可交互高保真原型 / 不新增前端组件：界面原型为静态 HTML 稿 + 文案说明。
- 不做市场定位、定价、GTM 分析：竞品调研仅「能力缺口」一个维度。
- 压测已否方向（记录防复活）：把 AgentFlow 扩成代码托管/IDE 平台（超定位）；把 BMAD/code-kit 开发流程绑进产品主线（违反 `CLAUDE.md`「不绑定特定开发流程或框架」）。

## 验收线（粗粒度，不是 AC）

1. 存在一份可浏览器直开的产品原型 + 界面原型文档，覆盖平台全部能力域（工作流监控 / Agent 编排 / 控制面与域管理 / 对话中心 / 工具库与模板市场 / 项目管理 / 角色系统 / 安全与审计），界面原型能呈现既有 32 页面的信息架构骨架并含调研新增界面。
2. 竞品调研产物覆盖至少 4 个点名产品、每条结论带来源引用；能力缺口清单逐条有去向：已融入原型 / 登记议题（需代码）/ 否决（带理由），零「写了没落」。
3. 原型文档与代码自洽：文中引用的模块与页面清单可在仓库中找到对应实现，可抽查验证。

## 风险与未知

- **【待人工决策 · R18①】** `product-design.html` 实际不存在于仓库任何分支（派发评论已核实），二选一：① 人工提供既有稿→在既有稿上融合；② 确认不存在→以 README + 代码为事实源新建。W3 产出形态强依赖此答案，**进入 2a-ui-design 前必须落定**；W1/W2 与 CHANGE 本身两路兼容，先行不阻塞。
- Buzzz / OpenWorker / AgentOS 名称泛，公开资料可能指代多个产品：1-requirement 调研时先消歧（锁定官网与定位）；查不到的标「待确认」并降级到人工提供材料，禁止幻觉结论（R6.2）。
- 小队无 D-discovery 主责成员（派发评论已报备）：调研五步内嵌进 1-requirement 执行，无 GD 议题门；是否补编由人工定，本 change 按现状推进。
- 「值得引入」判定受现有容量边界影响（FastAPI 单体 + SQLite 默认 + 单机部署）：1-requirement 需逐条做可行性粗判，避免原型写入实现不了的能力。

## 过程记录（0-change 执行轨迹）

- 步骤 0：change-id 自动生成 `product-prototype-refresh`（`.specs/` 下无冲突，未向人工索要 ID）。
- 步骤 0.4 架构级检测：**未命中**——纯文档/原型层变更，不增删项目级模块、不触 ADR、不改公共契约、不改容量边界、不涉及基础设施编排 → 跳过反问直接进 0.5。
- 步骤 0.5/0.6：命中前端项目（诉求含"界面原型"）；调性走强偏好例外锁定既有实现（见「视觉调性」段），未与反问同屏。
- 步骤 1 反问：唯一阻断性歧义（product-design.html 存在性）已由调度者按 R18① 提交人工；其余面向（诉求拆解、竞品清单、交付物层级）从 issue + 仓库事实已足，未新增提问。
- 步骤 1.5 压测（简化，逆向 + 消除两问）：若原型文档与代码事实源不锁定，产出即过时——故 W1 前置 + 验收线 3 设自洽要求；若允许"顺手改代码"，本 change 将膨胀为平台重构——故写盘边界硬性圈死。
- 步骤 2~5：影响面判定、范围排除、路径建议见上文各节。

---

> 后续 AC 与设计细节进入 `REQUIREMENT.md` / `DESIGN.md`，本文件不再扩展。
