# ⑤ 人工审核记录

## 提交记录

| 轮次 | 时间 | 提交内容 | 结论 |
|---|---|---|---|
| 1 | 2026-09-21 | 头脑风暴 + 竞品分析 + 结论 + 专家团裁决 | 待人工审核 |
| 2 | 2026-09-21 | 人工裁定「都落盘」 | **通过** |

## 人工裁定

**用户原话**：「都罗盘」→ 解释为「**都落盘**」，批准全部子议题产物写入磁盘。

## 审核门检查清单

| 检查项 | 状态 |
|---|---|
| R16.1 全程零代码改动 | ✅ 未修改 `scheduler_service.py` / `control_plane_api.py` / `reconcile_loop.py` / `database.py` |
| R16.2 事实依据 | ✅ 三处状态位置附 `文件:行号`；`npx tsc --noEmit \| grep -c "error TS"` = 32；`git ls-files` 计数可复现 |
| R16.3 人工审核门 | ✅ 裁定前未落盘 |
| R16.4 三轮上限 | ✅ 1 轮完成 |
| 结论可执行 | ✅ 含执行顺序、优先级、难度与追加约束 |

## 专家团留下的硬约束（落盘即生效）

1. **执行顺序是硬约束**：修构建 → 仓库卫生 → CI/LICENSE → 迁移框架 → 状态外置（架构师）。
2. **C2 须审查 git 历史是否含敏感文件**，必要时轮换凭据（安全审计师）。
3. **C4 迁移框架必须支持失败可观测**（安全审计师）。
4. **C5 租约三要素**：`lease_until` + 心跳续期 + 副本标识；**反对固定超时**（架构师）。
5. **C5 落地前先建行为基线测试**（研发负责人）——现有 8 个后端测试文件不足以覆盖队列/限流/退避语义。
6. **C5 不得过度承诺**：解决的是状态一致性，**不是恰好一次执行**（架构师）。
7. **C6（写明单副本限制）为最高优先级**，因不写会导致重复调和（安全审计师）。
8. **C8 排在 C1–C5 之后**（研发负责人）。

## 遗留待决

| 项 | 去向 |
|---|---|
| 该仓库是否曾推送到公共远端（决定 C2 历史审查的紧迫度） | 需人确认 |
| 是否接受 C5 前补测试导致的工期延长 | 需人确认 |
| 租约超时与心跳间隔的具体取值 | 设计阶段 |
| Docker/Helm 交付形态 | 登记为议题 |

> **2026-09-22 口径更正（code-kit 统一改造 R16.5）**：上表「→ 新 CHANGE」的去向统一改为**在 platform-evolution 本 change 目录补写 CHANGE.md 立项**（议题即 change，禁止另起第二个 change 留孤儿产物）；「转入下一轮」不变。原表文本作为历史审核记录保留不溯改。
