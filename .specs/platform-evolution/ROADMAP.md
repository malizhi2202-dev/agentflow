# ROADMAP — platform-evolution（第 1 轮 · 已过审结论汇总）

> 来源：本 change 已过审 5 个子议题的结论汇总（状态见 TOPICS.md）。**去重后按依赖关系重排**，不按子议题顺序。
> 详细依据见各子议题的 `D-<slug>/CONCLUSION.md`。

---

## 一、总览：5 个子议题的结论收敛为 3 个层次

| 层 | 含义 | 子议题 | 特征 |
|---|---|---|---|
| **地基** | 不修则其余全部无法安全落地 | S4 | 纯工程，成本低，风险低 |
| **正确性与安全** | 不修即缺陷（含确定性与条件性漏洞） | S3、S2(C1) | 部分是「把默认值改对」 |
| **能力补全** | 把已有优势做实、补齐缺失的一层 | S1、S5、S2(C2+) | 需新实体或配置化 |

**关键判断**：本轮的 5 个子议题不是并列的，**S4 是其余四项的前置**。
理由：状态不外置则多副本不可能（S2 的守护进程、S1 的团队级配额都要求多副本）；
构建不修复则任何自动化质量门都建立不起来（也就无法安全地做 S1/S3 的大改造）。

---

## 二、执行顺序（硬约束，非偏好）

```
第 1 步 · 让它能构建、能被贡献（S4 前半）
  ├─ 修复 32 个 TS 错误，恢复 npm run build          [S4-C1]
  ├─ git rm --cached node_modules + 审查 git 历史     [S4-C2]
  ├─ 补最小 CI（tsc + vitest + pytest）+ LICENSE      [S4-C3]
  └─ 写明「当前必须单副本部署」及原因                  [S4-C6]

第 2 步 · 消除确定性漏洞（S3 前半 + S2 表述）
  ├─ 消除 fail-open 身份回退（get_current_user）      [S3-C1] ← 全场最高优先级
  ├─ 口令哈希换自适应 + 拆分移除「重置为 123456」分支  [S3-C3a/C3b]
  ├─ 可信代理处理 + 启动日志与界面打印部署模式          [S3-C4]
  └─ 写明「Domain 不是安全边界」                       [S2-C1]

第 3 步 · 建立可信数据基础（S4 后半 + S5 前半）
  ├─ 合并两份 scanner 副本（纯重构）                   [S5-C1]
  ├─ 引入迁移框架（版本账本 + NOT VALID + 事务外执行）  [S4-C4]
  ├─ workflow.json 配置校验：响亮失败                  [S5-C4]
  └─ _detect_phase 未知态可观测                        [S5-C5]

第 4 步 · 状态外置（S4 后半，地基）
  ├─ 先建行为基线测试（队列/限流/退避语义）             [S4-C5 前置]
  └─ 状态外置 + 任务租约（lease_until + 心跳 + 副本标识）[S4-C5]

第 5 步 · 补齐能力层
  ├─ 引入 work_items 一等实体（change_id 为 join key） [S1-C1]
  ├─ 状态转换层门禁（Agent 不得自行置 done）           [S1-C2]
  ├─ work_item_events 时间线（与 audit.jsonl 分工）    [S1-C3]
  ├─ 身份改服务端签发（一次性切换，禁止双轨）           [S3-C2]
  ├─ 看/跑双谓词拆分（不与 S1-C1 并行）                [S3-C9]
  ├─ 域归属双点校验 + 统一 404                         [S3-C5]
  ├─ SSE 通道独立重新鉴权（单列设计任务）               [S3-C6]
  ├─ 审计 fail-closed                                  [S3-C7]
  ├─ 状态收敛是主防线（同事务）                         [S3-C8]
  ├─ 收件箱（排在 C1/C3 之后）                          [S1-C4]
  ├─ 阶段表抽到配置 + 前端读接口（C3 排在 C2 稳定后）    [S5-C2/C3]
  ├─ 凭据收敛：运行期不可达（验收标准）                  [S2-C2]
  └─ Makefile 统一入口                                  [S4-C7]
```

---

## 三、按优先级汇总（去重）

### P0 · 不修即缺陷（11 项）

| # | 事项 | 来源 | 难度 |
|---|---|---|---|
| 1 | 消除 fail-open 身份回退（`get_current_user` 缺身份→401 而非 admin） | S3-C1 | 低 |
| 2 | 修复 32 个 TS 错误，恢复 `npm run build` | S4-C1 | 低 |
| 3 | `git rm --cached node_modules` + 审查历史含敏感文件 | S4-C2 | 低 |
| 4 | 补最小 CI + LICENSE | S4-C3 | 低 |
| 5 | 写明「当前必须单副本部署」及三处内存状态 | S4-C6 | 低 |
| 6 | 写明「`Domain` 不是安全边界」 | S2-C1 | 低 |
| 7 | 口令哈希换自适应 + 移除「重置为 123456」分支（拆分验收） | S3-C3a/C3b | 低 |
| 8 | 可信代理处理 + 打印当前部署模式 | S3-C4 | 低 |
| 9 | 合并两份 scanner 副本 | S5-C1 | 中 |
| 10 | `workflow.json` 校验：拒绝未知字段 + 响亮失败 | S5-C4 | 低 |
| 11 | `_detect_phase` 未知态可观测（界面 + API） | S5-C5 | 低 |

### P1 · 地基与正确性（10 项）

| # | 事项 | 来源 | 难度 |
|---|---|---|---|
| 1 | 引入迁移框架（版本账本 + `NOT VALID` + 事务外执行 + fix-forward） | S4-C4 | 中 |
| 2 | 状态外置 + 任务租约（**地基**；前置：行为基线测试） | S4-C5 | 中 |
| 3 | 引入 `work_items` 一等实体（`change_id` 为 join key） | S1-C1 | 高 |
| 4 | 状态转换层门禁（动作者类型 × 目标状态） | S1-C2 | 中 |
| 5 | `work_item_events` 时间线（与 `audit.jsonl` 分工明确） | S1-C3 | 中 |
| 6 | 身份改服务端签发（一次性切换、写明移除日期） | S3-C2 | 中 |
| 7 | 域归属双点校验 + 越权与不存在统一 404 | S3-C5 | 低 |
| 8 | SSE 通道独立重新鉴权（单列设计任务） | S3-C6 | 中 |
| 9 | 审计 fail-closed + 状态收敛是主防线（同事务） | S3-C7/C8 | 低-中 |
| 10 | 凭据收敛：验收标准＝**运行期不可达** | S2-C2 | 中 |

### P2 · 能力与交付（7 项）

| # | 事项 | 来源 | 难度 |
|---|---|---|---|
| 1 | 看/跑双谓词拆分（不与 S1-C1 并行） | S3-C9 | 中 |
| 2 | 收件箱（只在需人拍板时通知，排在 C1/C3 后） | S1-C4 | 低 |
| 3 | 阶段表抽到配置（配置为唯一权威） | S5-C2 | 中 |
| 4 | 前端 6 处改为读接口（排在 C2 稳定后） | S5-C3 | 中 |
| 5 | 配置版本迁移（prune 废弃字段） | S5-C6 | 低 |
| 6 | 委托式隔离抽象（降级须三处显式标记 `isolation: none`） | S2-C3 | 高 |
| 7 | 出口默认拒绝 + 工作负载身份（覆盖全部出网路径） | S2-C4 | 中高 |

---

## 四、登记为议题（本轮不做，R18.4）

| 议题 | 来源 | 暂缓理由 |
|---|---|---|
| Team/Squad 一等对象 + 团队级配额与审计 | S1-C5 | 需先有 `work_items`（路线图空白 W-2） |
| 守护进程模型（Agent 跑在用户机器） | S2-C5 | 依赖 S4-C5 的状态外置与租约，联合设计 |
| 沙箱运行时选型 | S2-C3 | 业界五条路线无标准，待 S4 完成后决策 |
| 工作负载身份（SPIFFE）是否引入 | S2-C4 | 依赖域模型扩展 |
| Docker/Helm 交付形态 | S4-C8 | 先有 CI 与统一入口 |
| 是否引入 OIDC 作为可选增强 | S3 | 与「clone 即跑」冲突，需产品确认 |
| 语义（NLP）路由 / 多集群联邦 / 计费结算 | §1.4 | 产品已明确列为不做或远景 |

---

## 五、必须由人裁定的取舍（6 项）

这 6 项**不是技术选择**，需要人确认产品承诺的边界：

| # | 取舍 | 来源 | 影响 |
|---|---|---|---|
| 1 | **是否允许用户自定义阶段模型** | S5 | 允许→改代码兑现定位；不允许→**必须改定位表述**。本轮唯一的定位级取舍 |
| 2 | C2 门禁是否改变现有自动流转行为 | S1 | 「Agent 不得自行置终态」会改变既有行为 |
| 3 | `work_items` 与现有 `projects` 合并还是独立 | S1 | 影响数据模型与迁移 |
| 4 | 是否接受「未知身份一律拒绝」的可用性变化 | S3 | 今天「不带任何头即以 admin 使用」会被移除，可能影响本地脚本 |
| 5 | 是否接受 C5 前补测试导致的工期延长 | S4 | 研发负责人的前置条件 |
| 6 | 该仓库是否曾推送到公共远端 | S4 | 决定 git 历史审查与凭据轮换的紧迫度 |

---

## 六、本轮的方法性结论

1. **「有代码」≠「有效果」**：Multica 的 `done` 门只有提示词契约、服务端零校验；
   AgentFlow 的 `npm run build` 脚本存在但必然失败。两者同构——
   **验证标准必须是可执行的，不是「存在即完成」。**
2. **「为什么」比「是什么」更可移植**：竞品最有价值的产出是注释里的理由
   （如 admin 越权读邮箱的具体路径、WS 层为何必须独立鉴权），
   而非机制本身。
3. **空白结论有保鲜期**：本领域 3–6 个月即需重做调研
   （Google AX 2026-03 才创建、kagent 2026-09 才发 alpha1、Roo Code 已关停、Continue 已被收购）。
4. **必须承认定位更正**：原假设「K8s 式 Agent 管控无人做过」**已被推翻**——
   kagent（CNCF）、Google AX、agent-sandbox 都已在这条路上。
   差异化应建立在它们**共同的空白**上（动作级审计、任务队列+防饿死、知识产物监控、零依赖可跑）。

---

## 第 3 轮新增过审结论（2026-09-22 · S6 + S7 · 用户原话「都通过」）

### 定位级（进文档 §1/§1.6 已执行）
1. **六件套 = 品类入场券**（工作项+负责人 / 收件箱+审批门 / 可回放时间线 / 成本归因 / runtime 注册 / 协议化——头部产品 2026-09 全部齐备，multica 51,081★·月 6 release 居首）；**胜负手在三块零先例空白**：画布↔YAML 双向对账（最强竞品只有只读 Mermaid）/ 产物谱系+晋级门禁（品类无对应物）/ **域=配额+沙箱+RBAC 三合一强制边界（无人组装，提为头名空白）**。
2. **诚实性定位句**：无沙箱可以是明写的产品决定（multica 同型：逻辑域+真边界在执行宿主，且把边界下沉到渲染端 iframe sandbox），但不能是没人说清的默认状态——AgentFlow v3 在"Domain 不是安全边界"事实处落此句。
3. **模板市场库内自证**："流程=可挂载技能"已在竞品环境跑通（本机 multica 实例把 9 阶段流程做成 20 个 kit-* skill 挂 34 agent）——知识产物 + 模板市场不是空想。

### 路线图新条（进 §1.6.2 P1 已执行）
4. **P1-12 审批语义协议对齐预留**：人批门禁状态机按 A2A HITL 扩展语义设计映射（input-required 暂停→同 task 延续），只预留状态语义不承诺协议实现（A2A v1.1 Elicitation #2149/#2143 在途是移动靶）。
5. **P1-13 成本归因到人**：originator（只鉴权）/ accountable（只审计·成本）双列 + 8 级归属瀑布 + 域策略级 fail-closed 开关（解析不到责任人拒跑不降级）。

### 协作与原型语义（p16-p22 注记已执行）
6. **p19 从开放问题改"业界有成型解"**：阶段=目录表+4 语义锚（unstarted/started/done/closed），自定义阶段只继承生命周期**禁授行为**、category 建后不可改（multica 迁移 332/337 实证）——待裁定 #1 决策成本降为"采不采纳此方案"。
7. **status × triage 双正交轴**（"到哪一步"≠"算不算活"；门只拦推导执行者不拦人点名）；**运行枢纽记账面**（多上下文挂靠、计划 vs 收据双数组、会话续用键=(agent,issue) 含 cancelled 排 poisoned）。
8. **《全局交互纪律》12 条**成文进 §6 开头：统一详情骨架（inspector 三态+?view= 分段+页内 CapabilityBanner）/ 视图即链接（筛选快照+URL 双向同步）/ Runtime 健康四态+阈值入产品文案 / 失败三件套（Retry with context·Edit as advanced form·升级到人）+Run 四道门拒因 toast / Quick Actions 命名 prompt 一键跑 / 权限 `{allowed,reason}` 纯函数渲染 / 草稿与滚动 memento 一等状态 / 产物预览 iframe 渲染端边界 / 留痕必须"表+界面"两步齐（multica 有表无界面为戒）/ 三页规划原型补注记。
9. **工程纪律 §1.6.5 由 2 条扩 8 条**：新增 done-需-证据+per-card budget（amux）/ 干预记变更前快照（AgentTeams 1.2.3）/ 不可变 principal（编辑触发器≠重新授权）/ 合并重打身份（MUL-4195）/ 改一处说明同次同步引用处 / 实时契约（WS 降延迟 DB 最终态、推送与查询同形状）。
10. **盯防表重写**（multica 头号；K8s 阵营一行——agent-sandbox v1.0 GA+ax v0.3.0+kagent×substrate gVisor/microVM×Kueue 已互打；OpenHands Canvas/AgentTeams/amux/ClawManager/dsh-agent-teams 入列；AutoGen 维护态·swarms/MetaGPT 停更降级出表）；**FAQ 补两路线分野**（IM 内核 OCTO / 嵌入式团队层 dsh-agent-teams 与管控面对位）。

### 降档与否决记录（工程建议清单，不进文档正文）
- 进 ROADMAP 工程建议：三版本策略语义（schema/policy/subscription）· i18n parity 测试+代理式 t() · 声明式快捷键表 · .env 逐变量带权衡 · DB 化调度器 sys_cron_execution 样式（解"三处进程内存"的成型参照）。
- 否决：全量抄 115 实体表结构 / acceptance_criteria 有列无校验类债照学 / 导航 IA 重组 / 协议实现追进（映射预留即止）/ 注册中心进正文（存疑：无人做成可能是需求未验证，留观察）。
