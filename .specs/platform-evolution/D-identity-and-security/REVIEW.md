# ⑤ 人工审核记录

## 提交记录

| 轮次 | 时间 | 提交内容 | 结论 |
|---|---|---|---|
| 1 | 2026-09-21 | 头脑风暴 + 竞品分析 + 结论 + 专家团裁决 | 待人工审核 |
| 2 | 2026-09-21 | 人工裁定「都落盘」 | **通过** |

## 人工裁定

**用户原话**：「都罗盘」→ 解释为「**都落盘**」，批准全部子议题产物写入磁盘。

## 审核门检查清单

| 检查项 | 状态 |
|---|---|
| R16.1 全程零代码改动 | ✅ 仅读取 `main.py` / `auth.py`，未修改 |
| R16.2 事实依据 | ✅ 全部附 `文件:行号`；grep 无 `X-Forwarded-For` 命中等均给出可复现命令 |
| R16.3 人工审核门 | ✅ 裁定前未落盘 |
| R16.4 三轮上限 | ✅ 1 轮完成 |
| 结论可执行 | ✅ 含优先级、难度、拆分与验收标准 |

## 专家团留下的硬约束（落盘即生效）

1. **C1 是全场最高优先级**——fail-open 回退 admin 是确定性后门（安全审计师）。
2. **C2 禁止长期双轨**，须写明 `X-User-Id` 移除日期（架构师）。
3. **C3 拆为 C3a/C3b**，其中「重置为 123456」分支是**独立提权路径**（安全审计师）。
4. **C4 必须在启动日志与界面打印当前模式**（安全审计师）。
5. **C6 单列设计任务**，不是顺手加一行（研发负责人）。
6. **C8 同事务为硬要求**，分事务会留下可利用窗口（安全审计师）。
7. **C9 不与 S1-C1 并行**推进（架构师）。

## 遗留待决

| 项 | 去向 |
|---|---|
| 部署形态默认值（localhost vs behind-proxy） | 需产品确认 → 新 CHANGE |
| 「未知身份一律拒绝」对既有脚本的影响面 | 需清点 → 新 CHANGE |
| 令牌形态最终选择（opaque vs JWT） | 设计阶段 |
| 是否引入 OIDC 作为可选增强 | 登记为议题 |
| SSE 订阅授权模型设计 | 新 CHANGE（与 C6 绑定） |

> **2026-09-22 口径更正（code-kit 统一改造 R16.5）**：上表「→ 新 CHANGE」的去向统一改为**在 platform-evolution 本 change 目录补写 CHANGE.md 立项**（议题即 change，禁止另起第二个 change 留孤儿产物）；「转入下一轮」不变。原表文本作为历史审核记录保留不溯改。
