# ③ 结论与建议 — S7（批判技法：预演失败——假设 v3 照抄全量，哪先断？）

**预演断点**：照抄全量会让 v3 变成"multica 说明书"——它的表是为 115 实体/537 迁移的规模长出来的，AgentFlow 是 117 个 py 文件的单进程平台。收敛为**只借"设计语义"不借"实现体量"**：

**结论（v3 落点，待 ⑤ 裁决）**：
1. **p19 阶段模型配置：从"开放问题"改"有业界成型解"**——目录表+4 语义锚+自定义只继承生命周期禁授行为（multica 迁移 332/337 实证）。待裁定 #1 的决策成本从"要不要做"降为"要不要采纳这个成型方案"。
2. **§1.6.5 工程纪律 +3 条**：不可变 principal（编辑触发器≠重新授权）；合并重打身份；改一处说明必须同次同步引用处（multica AGENTS.md 原话律，与 kit R15 同构互证）。
3. **协作三页原型（p20/p21/p22）各补设计注记块**：双正交轴与"待受理"分区 / 提醒分层三规则（值得打扰才 ping、fail-open 宁多发、旧失败自愈归档）/ 运行详情记账面（5 上下文挂靠、计划 vs 收据、wait_reason 徽标、取消人署名、截断标记）。
4. **§1.6.2 P1 补一条 P1-13「成本归因到人」**：originator/accountable 双列+fail-closed 开关进域策略——把"成本按 agent/issue 归因"（S6 入场券）再深一层"每笔有唯一可解释的人类责任人"。
5. **诚实性升格（第 10 候选）**：v3 在"Domain 不是安全边界"事实处加一句 multica 同型先例——"无沙箱可以是一个明写的产品决定（真边界=执行宿主），但不能是一个没人说清的默认状态"。竞品对照句：multica 逻辑域+K8s 阵营真沙箱，AgentFlow 的"域=配额+沙箱+RBAC 三合一"空白判断（S6）**获两侧夹击验证**。
6. **模板市场叙事补库内自证（G）**："流程=可挂载技能"在竞品环境跑通（20 skill×34 agent）——AgentFlow 工作流知识产物+模板市场不是空想，品类里已有人这么玩。
7. **否决记录**：全量抄表结构（#11/#12 降档）；写进 ROADMAP 执行项的只有语义注记，不含 multica 特有实现（triage、seat capacity、composio 等域外物）。

**建议**：全部为文档/原型标注层，零代码改动（R16.1）。与 S6 的 7 条落点合并为一次 v3 重写，顺序：盯防/空白（事实）→ 纪律/定位（结论）→ 三页原型注记 → 元数据。


## ③-B 结论补遗（UI 层增量，GD 增量重投见 PANEL）

在 ③ 的 7 条之上追加：

8. **§6 原型章开头新增《全局交互纪律》小节（8 条）**：①所有详情共用"左 inspector(自动保存三态)+右 ?view= 分段，无权限页内横幅不改 URL"骨架（落 p4/p7/p14/p18 注记）②保存视图=筛选快照+URL 双向同步⇒**视图即链接**（p6/p15/p12）③Runtime 健康四态+阈值写进产品文案（p10/p22）④失败卡片固定三件套 Retry with context / Edit as advanced form / 升级到人，Run now 四道门拒因原样 toast（p11/p17）⑤Quick Actions 命名 prompt→动作、点击即跑、use_count（p7/p14/p19）⑥权限渲染 `{allowed, reason}` 纯函数规则表（p16 及全部页）⑦草稿/滚动 memento 一等状态（p18/p19）⑧**产物预览 iframe sandbox——`.specs/` HTML 渲染边界在显示端**（p9/p18，安全注记）。
9. **#8 补后半句**：留痕类改动必须"表+界面"两步齐（multica 规则版本表有、详情页无展示，为戒）。
10. **降档记录**：i18n parity/声明式快捷键/env 带权衡三件 → ROADMAP 工程建议清单；导航 IA 重组 → 否决（无迁移理由）。


## ③-C 终版合并净增落点（10 条之上 +2）

11. **§1.6.5 纪律第 6 条**：实时推送契约——WS 只降延迟、DB 为最终态、推送事件与查询接口同形状（AgentFlow 的 `runtime.jsonl` 监控流与 SSE 现状正缺这条，multica 用事故注释写明理由）。
12. **p16 RBAC 注记补三句**：私有资源连管理员也不旁路；枚举值可预留但**未实装的授权位必须显式 INERT**；"可见性"若与"可调用目标"并存，后者为唯一鉴权源、前者降级为展示字段。
（②-C.3 实测分布作为落点 #3 的论据入文；②-C.4 三版本语义随 #16 降档进 ROADMAP 工程建议。）
